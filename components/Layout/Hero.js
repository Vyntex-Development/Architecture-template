import Link from "../UI/Link";

const Hero = ({ title, description, url }) => {
  return (
    <div className="container">
      <h1></h1>
      <p></p>
      <div>
        <Link href={"/"} />
        <Link href={"/"} />
      </div>
    </div>
  );
};

export default Hero;
